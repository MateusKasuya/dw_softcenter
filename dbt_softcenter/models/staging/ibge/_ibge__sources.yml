version: 2

sources:

  - name: raw
    description: "Base de municípios ibge obtida através da API do IBGE"
    database: postgres
    schema: raw
    tables:
      - name: ibge_municipios
        description: "Base csv inserida pelo dbt seed"
